# workshop-frontend

Plain HTML/CSS/JS todo app. No build step. Requires the backend running at http://localhost:8000 (see `../backend/README.md`).

## Run

Open `index.html` directly in a browser, or serve it:

```bash
python -m http.server 5500
```

Then visit http://localhost:5500.
